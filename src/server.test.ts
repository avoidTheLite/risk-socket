import createWebSocketServer from "./common/util/createWebSocketServer";
import { beforeAll, beforeEach, afterAll, describe, it, expect } from '@jest/globals';

describe('Websocket Server', () => {
    beforeAll(async () => {
        // Start server
    })
    
    afterAll(async () => {
        // Stop server
    })
    
    it('should initialize the socket server', () => {
        // 1. Create test client
        // 2. Send a client message to the server
        // 3. Assert that client receives correct response
        // 4. Close client
    })
})